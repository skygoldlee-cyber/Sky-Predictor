async def sample(api):
    inputs = {
        't8433InBlock': {
            'dummy': '', # Dummy
        },
    }
    response = await api.request('t8433', inputs)
    if not response: return print(f'요청 실패: {api.last_message}')
    
    print(response)
